---
id: "1.1.1"
title: "Implementar Supabase Auth (login/session)"
status: Ready
epic: "EPIC-1"
sprint: "Sprint 1"
agent: "@dev"
effort: "4h"
priority: CRITICAL
created_at: "2026-03-16"
dependencies:
  - "1.0.1"
  - "1.0.2"
  - "1.0.3"
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

# Story 1.1.1 — Implementar Supabase Auth (login/session)

**Status:** Ready | **Sprint 1 — Security Foundation** 🔴 | **Agente:** @dev | **Esforço:** 4h

> **Bloqueante de segurança:** Sem autenticação, as RLS policies da Story 1.1.3 não podem funcionar (retornam NULL para `auth.uid()`). Esta story é o pré-requisito para fechar todos os vetores de ataque críticos.

---

## Story Statement

**Como** Carlos (CEO / único usuário do Nexus OS),
**Quero** ser obrigado a fazer login com email/senha antes de acessar o dashboard,
**Para que** somente eu possa acessar os dados de negócio (leads, calls, OKRs, métricas), e as políticas de Row Level Security do Supabase funcionem corretamente.

---

## Contexto

Hoje o Nexus OS não possui nenhum sistema de autenticação. Qualquer pessoa que acesse a URL do app tem acesso total a todos os dados. Combinado com a anon key exposta (Story 1.0.2) e sem RLS ativo (Story 1.1.3), o banco de produção está completamente aberto.

Débitos remediados: **TD-C3** (sem autenticação), **TD-C4** (RLS ineficaz sem `auth.uid()`).

O Nexus OS tem apenas **um usuário** (Carlos, o CEO). Não há necessidade de multi-tenant, roles complexos, ou registro público. A solução é Supabase Auth com email/senha para um único usuário.

[Source: docs/architecture/technical-debt-assessment.md#td-c3-td-c4]
[Source: docs/architecture/system-architecture.md#overview]

---

## Acceptance Criteria

- [ ] **AC-1:** Acesso a qualquer rota do dashboard redireciona para tela de login se não autenticado
- [ ] **AC-2:** Tela de login com campos email e senha funciona com Supabase Auth
- [ ] **AC-3:** Após login bem-sucedido, usuário é redirecionado ao dashboard
- [ ] **AC-4:** Sessão persiste entre reloads (localStorage/cookie gerenciado pelo Supabase client)
- [ ] **AC-5:** Botão de logout disponível (ex: no Sidebar ou Header) — limpa sessão e redireciona para login
- [ ] **AC-6:** `auth.uid()` retorna UUID válido do usuário logado (necessário para Story 1.1.3)
- [ ] **AC-7:** Se credenciais inválidas, mensagem de erro é exibida ao usuário
- [ ] **AC-8:** Interface visual de login é consistente com o design dark theme do Nexus OS
- [ ] **AC-9:** Nenhuma regressão visual nas 9 páginas após implementação

---

## Tasks / Subtasks

- [ ] **Task 1 — Criar usuário no Supabase Auth** (AC: 2)
  - Via Supabase Dashboard → Authentication → Users → Add user
  - Criar: email de Carlos + senha forte
  - Anotar UUID do usuário (será o `auth.uid()` fixo para as RLS policies)

- [ ] **Task 2 — Criar componente `LoginPage.tsx`** (AC: 2, 7, 8)
  - Criar `components/auth/LoginPage.tsx`
  - Formulário: campos email + senha + botão "Entrar"
  - Chamar `supabase.auth.signInWithPassword({ email, password })`
  - Exibir erro se autenticação falhar
  - Estilo: dark theme consistente com o resto do app (bg-gray-900, text-white)

- [ ] **Task 3 — Criar `AuthProvider` / hook `useAuth`** (AC: 1, 4, 6)
  - Criar `components/auth/AuthProvider.tsx` com Context
  - Usar `supabase.auth.getSession()` para estado inicial
  - Usar `supabase.auth.onAuthStateChange()` para reatividade
  - Exportar hook `useAuth()` que retorna `{ user, session, loading }`

- [ ] **Task 4 — Criar `ProtectedRoute` wrapper** (AC: 1, 3)
  - Criar `components/auth/ProtectedRoute.tsx`
  - Se `loading`: mostrar spinner/loading state
  - Se `!user`: renderizar `<LoginPage />`
  - Se `user`: renderizar children (o dashboard)

- [ ] **Task 5 — Integrar em `App.tsx`** (AC: 1, 3, 9)
  - Envolver o app com `<AuthProvider>`
  - Envolver o dashboard com `<ProtectedRoute>`
  - Verificar que todas as 9 páginas continuam funcionando após login

- [ ] **Task 6 — Implementar logout** (AC: 5)
  - Adicionar botão de logout no Sidebar (ou Header)
  - Chamar `supabase.auth.signOut()` ao clicar
  - Redirecionar para `<LoginPage />` após logout

- [ ] **Task 7 — Testar fluxo completo** (AC: 1-9)
  - Testar: URL direta → redireciona para login
  - Testar: login com credencial inválida → mensagem de erro
  - Testar: login com credencial válida → acessa dashboard
  - Testar: reload da página → sessão mantida
  - Testar: logout → volta para login
  - Testar: todas as 9 páginas carregam corretamente após login

- [ ] **Task 8 — Commit**
  - `git commit -m "feat: implementar Supabase Auth com login/session [Story 1.1.1]"`

---

## Dev Notes

### Stack e padrões do projeto

```
React 19.2.3 + TypeScript ~5.8.2 + Vite ^6.2.0
Supabase client: @supabase/supabase-js 2.39.3
```
[Source: docs/architecture/system-architecture.md#tech-stack]

### Arquitetura atual — App.tsx como God Component

```
App.tsx (God Component)
├── 7x useState (global state)
├── fetchData() — 7 queries paralelas Supabase
├── Event bus: window.dispatchEvent('nexus-data-updated')
└── Tab routing: useState('activeTab')
```

**IMPORTANTE:** Não refatorar o App.tsx nesta story. A reestruturação do God Component é escopo do Sprint 2 (Stories 1.2.1, 1.2.2, 1.2.3). Esta story apenas adiciona a camada de auth POR CIMA da estrutura existente.

[Source: docs/architecture/system-architecture.md#application-architecture]

### Padrão de implementação do AuthProvider

```typescript
// components/auth/AuthProvider.tsx
import { createContext, useContext, useEffect, useState } from 'react'
import { Session, User } from '@supabase/supabase-js'
import { supabase } from '@/lib/supabase'

interface AuthContextValue {
  user: User | null
  session: Session | null
  loading: boolean
}

const AuthContext = createContext<AuthContextValue>({
  user: null, session: null, loading: true
})

export function AuthProvider({ children }: { children: React.ReactNode }) {
  const [user, setUser] = useState<User | null>(null)
  const [session, setSession] = useState<Session | null>(null)
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    supabase.auth.getSession().then(({ data: { session } }) => {
      setSession(session)
      setUser(session?.user ?? null)
      setLoading(false)
    })

    const { data: { subscription } } = supabase.auth.onAuthStateChange((_event, session) => {
      setSession(session)
      setUser(session?.user ?? null)
    })

    return () => subscription.unsubscribe()
  }, [])

  return <AuthContext.Provider value={{ user, session, loading }}>{children}</AuthContext.Provider>
}

export const useAuth = () => useContext(AuthContext)
```

### Estrutura de arquivos a criar

```
components/
└── auth/
    ├── AuthProvider.tsx    ← Context + hook useAuth
    ├── LoginPage.tsx       ← Tela de login
    └── ProtectedRoute.tsx  ← Guard de rota
```

### Visual da LoginPage

Deve seguir o dark theme do app:
- Background: `bg-gray-900` ou `bg-slate-900`
- Card central com: logo/nome "Nexus OS", campos email/senha, botão "Entrar"
- Sem registro (apenas login — usuário único)
- Consistent com framer-motion para animações de entrada (opcional mas preferível)

[Source: docs/architecture/frontend-spec.md]

### Sem React Router nesta story

React Router é instalado no Sprint 2 (Story 1.2.1). Por ora, o routing continua via `useState('activeTab')` no App.tsx. O `ProtectedRoute` apenas condicionalmente renderiza `<LoginPage />` ou o dashboard, sem URL routing.

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled em `core-config.yaml`.
> Quality validation will use manual review process only.

---

## Definition of Done

- [ ] Login funciona com email/senha
- [ ] Sessão persiste entre reloads
- [ ] Logout funciona e limpa sessão
- [ ] Acesso direto sem auth → redirect para login
- [ ] `auth.uid()` retorna UUID válido (confirmado via DevTools → Supabase)
- [ ] Todas as 9 páginas continuam funcionando
- [ ] Commit realizado

---

## Handoff

**Próximo:** @po `*validate-story-draft 1.1.1`
**Após conclusão:** Unblocks 1.1.3 (RLS precisa de auth.uid() ativo)
**Paralela:** 1.1.2 pode iniciar simultaneamente (independente de 1.1.1)
