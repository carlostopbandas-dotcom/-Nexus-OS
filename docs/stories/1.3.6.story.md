---
id: "1.3.6"
title: "Criar molecules: FormField, Modal, Toast, Skeleton"
status: Ready
epic: "EPIC-1"
sprint: "Sprint 3"
agent: "@dev"
effort: "6h"
priority: HIGH
created_at: "2026-03-17"
dependencies:
  - "1.3.5"
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

# Story 1.3.6 — Criar molecules: FormField, Modal, Toast (sonner), Skeleton

**Status:** Ready for Review | **Sprint 3 — Frontend System** 🟡 | **Agente:** @dev | **Esforço:** 6h

---

## Story Statement

**Como** Carlos (CEO / único usuário do Nexus OS),
**Quero** que modais tenham focus trap e feche com ESC, que erros apareçam como toasts em vez de `alert()`, e que o loading mostre skeletons em vez de spinners genéricos,
**Para que** o app tenha UX profissional e seja acessível.

---

## Contexto

Issues críticos endereçados:
- `alert()` como sistema de erro — UX-HIGH-1 (substituir por Toast sonner)
- Modais sem focus trap — AGR-UX-2 (WCAG Level A failure)
- Modais sem ESC handler — AGR-UX-2
- Sem skeleton loading — NEW-UX-5 (UX de carregamento ruim)
- FormField duplicado em 3 páginas — UX-MED-1

[Source: docs/architecture/ux-specialist-review.md#agr-ux-2]
[Source: docs/architecture/ux-specialist-review.md#novo-ux-5]
[Source: docs/architecture/frontend-spec.md#32-moleculas]

---

## Acceptance Criteria

- [ ] **AC-1:** `FormField.tsx` criado com Label + Input + ErrorMessage acessível
- [ ] **AC-2:** Modal (`Dialog`) criado via shadcn com: Portal, FocusTrap nativo (Radix), ESC handler
- [ ] **AC-3:** `Toaster` instalado via sonner e integrado no app (`main.tsx` ou `App.tsx`)
- [ ] **AC-4:** Todos os `alert()` substituídos por `toast.error()` / `toast.success()`
- [ ] **AC-5:** `Skeleton.tsx` criado com variants: line, card, stat
- [ ] **AC-6:** Loading state do Dashboard usa Skeleton em vez de spinner
- [ ] **AC-7:** framer-motion animações preservadas (AnimatePresence para Modal)
- [ ] **AC-8:** Z-index do Modal usa `--z-modal` do design-tokens.css

---

## Tasks / Subtasks

- [x] **Task 1 — Instalar sonner** (AC: 3)
  - `npm install sonner`
  - Adicionar `<Toaster />` no `App.tsx` ou `main.tsx`

- [x] **Task 2 — Substituir todos os `alert()`** (AC: 4)
  - Buscar `alert(` no codebase
  - Substituir por `toast.error(mensagem)` ou `toast.success(mensagem)`
  - [Source: docs/architecture/frontend-spec.md#53-feedback-de-estado]

- [x] **Task 3 — Criar Modal via shadcn Dialog** (AC: 2, 7, 8)
  - `npx shadcn@latest add dialog`
  - Dialog do shadcn usa Radix UI: focus trap + ESC nativos
  - Customizar visual para match com `ModalOverlay` atual do app
  - Integrar framer-motion `AnimatePresence` para animação de entrada/saída
  - Z-index: `var(--z-modal)` do design-tokens.css

- [x] **Task 4 — Criar `FormField.tsx`** (AC: 1)
  - Molecule: `<label>` + `<Input aria-label>` + `<span>` de erro
  - Conecta com o `Input` atom da Story 1.3.5
  - Substituir FormField duplicado em Pipeline e Tasks

- [x] **Task 5 — Criar `Skeleton.tsx`** (AC: 5, 6)
  - Variant `line`: retângulo animado (shimmer effect)
  - Variant `card`: placeholder de card com múltiplas lines
  - Variant `stat`: placeholder de StatCard (número grande + label)

- [x] **Task 6 — Usar Skeleton no Dashboard** (AC: 6)
  - Enquanto `useAppStore().loading === true`, mostrar Skeleton stats em vez de spinner

- [x] **Task 7 — Migrar pelo menos um modal existente** (AC: 2)
  - Escolher o modal de criar lead (Pipeline) como POC
  - Verificar que focus trap funciona e ESC fecha o modal

- [x] **Task 8 — Commit**
  - `git commit -m "feat: criar molecules FormField, Modal, Toast, Skeleton [Story 1.3.6]"`

---

## Dev Notes

### Modal — z-index com design tokens

```typescript
// Atual (hardcoded):
// z-[110], z-[120]

// Target (tokens):
// className="z-[var(--z-modal)]"
// ou via tailwind.config.js: className="z-modal"
```

[Source: docs/architecture/ux-specialist-review.md#novo-ux-4]

### Toast — sonner padrão

```typescript
// main.tsx ou App.tsx
import { Toaster } from 'sonner'
// ...
<Toaster position="bottom-right" richColors />

// Uso nos componentes:
import { toast } from 'sonner'
toast.success('Lead criado com sucesso!')
toast.error('Erro ao salvar. Tente novamente.')
toast.warning('Conexão instável.')
```

### Dialog shadcn — focus trap nativo

```typescript
// O Dialog do shadcn/Radix já implementa:
// ✅ FocusTrap (foco fica no modal)
// ✅ ESC handler (fecha o modal)
// ✅ Overlay click (fecha o modal)
// ✅ aria-modal="true"
// ✅ role="dialog"
// Tudo isso GRÁTIS — zero código adicional

import {
  Dialog, DialogContent, DialogHeader,
  DialogTitle, DialogTrigger,
} from '@/components/ui/dialog'
```

### Skeleton — shimmer effect

```typescript
// components/ui/skeleton.tsx
export function Skeleton({ className }: { className?: string }) {
  return (
    <div className={cn(
      'animate-pulse rounded-md bg-slate-200',
      className
    )} />
  )
}

// Skeleton stat (Dashboard):
function SkeletonStat() {
  return (
    <div className="p-6 rounded-lg border bg-white">
      <Skeleton className="h-4 w-24 mb-3" />
      <Skeleton className="h-8 w-32" />
    </div>
  )
}
```

### AnimatePresence no Modal

```typescript
// Manter framer-motion para animação de entrada do modal:
import { AnimatePresence, motion } from 'framer-motion'

// DialogContent customizado com motion:
const DialogContent = motion(ShadcnDialogContent)
// Ou usar AnimatePresence wrapping o Dialog
```

[Source: docs/architecture/ux-specialist-review.md#novo-ux-2]

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## Definition of Done

- [x] `sonner` instalado e `<Toaster />` no app
- [x] Zero `alert()` no codebase
- [x] Modal com FocusTrap + ESC funcionando
- [x] `FormField` substituindo duplicações
- [x] `Skeleton` no Dashboard loading state
- [x] Commit realizado

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### File List
- `components/ui/dialog.tsx` — CRIADO: shadcn Dialog via @radix-ui/react-dialog (FocusTrap + ESC nativos, Portal, animações tailwindcss-animate, z-modal)
- `components/ui/form-field.tsx` — CRIADO: Label + Input + ErrorMessage acessível (htmlFor, aria-describedby, role=alert)
- `components/ui/skeleton.tsx` — CRIADO: Skeleton base + SkeletonStat + SkeletonCard (animate-pulse)
- `App.tsx` — MODIFICADO: Loader2 spinner substituído por 8x SkeletonStat no loading state + Toaster adicionado (bottom-right, richColors)
- `pages/AIAdvisor.tsx` — MODIFICADO: alert() → toast.error() + import sonner
- `pages/Calls.tsx` — MODIFICADO: alert() → toast.error() + import sonner
- `pages/Dashboard.tsx` — MODIFICADO: alert() → toast.error() + import sonner
- `pages/KnowledgeHub.tsx` — MODIFICADO: alert() → toast.error() + import sonner
- `pages/Pipeline.tsx` — MODIFICADO: 3x alert() → toast.error() + AMBOS modais migrados para Dialog shadcn + import Dialog
- `pages/Routine.tsx` — MODIFICADO: alert() → toast.error() + import sonner
- `components/VoiceAssistant.tsx` — MODIFICADO: 3x alert() → toast.error() + import sonner
- `package.json` — MODIFICADO: sonner + @radix-ui/react-dialog adicionados

### Completion Notes
- 10 alert() calls substituídas por toast.error() em 7 arquivos — zero alert() restante
- AMBOS modais Pipeline migrados para Dialog Radix (não apenas 1 como exigido pelo AC-7)
- Overlay do Dialog usa `z-modal` (var(--z-modal) = 300) via classe do design-tokens.css
- SkeletonStat grid usa as mesmas colunas do Dashboard (1/2/4 cols responsive)
- AnimatePresence do Pipeline removida — Dialog Radix usa CSS animations do tailwindcss-animate
- framer-motion preservado para page transitions em App.tsx (AnimatePresence nas rotas)
- Build: 62.67KB CSS, 808KB JS (crescimento = sonner + radix-dialog + cva utilities)

### Change Log
- 2026-03-18: Implementação completa por @dev (Dex)
