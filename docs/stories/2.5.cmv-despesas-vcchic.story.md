---
id: "2.5"
title: "Registro Manual de CMV + Despesas — Grupo VcChic"
status: Ready for Review
epic: "EPIC-2"
sprint: "Sprint 2.5"
agent: "@dev"
effort: "3h"
priority: MEDIUM
created_at: "2026-05-07"
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: [code_review, test_coverage, rls_access]
dependencies: ["2.4"]
---

# Story 2.5 — Registro Manual de CMV + Despesas (Grupo VcChic)

**Status:** Draft | **Sprint 2.5** | **Agente:** @dev | **Esforço:** 3h

---

## Story Statement

**Como** CEO do Nexus OS,
**Quero** registrar manualmente CMV (Custo de Mercadoria Vendida) e despesas das lojas VcChic, Moriel e Sezo,
**Para que** eu tenha uma visão completa dos custos e possa acompanhar o lucro real de cada loja no Dashboard Financeiro.

---

## Contexto

### O que já está feito (não requer implementação)

| Artefato | Status | Relevância |
|---------|--------|-----------|
| `supabase/migrations/011_financial_entries.sql` | ✅ Ativo | Tabela `financial_entries` com RLS configurado |
| `services/financialService.ts` | ✅ Ativo | `getStoreFinancials` + `getGroupSummary` |
| `pages/FinancialDashboard.tsx` | ✅ Ativo | Dashboard com KPI cards — recebe nova seção "Lançamentos" |
| `components/auth/RoleGuard.tsx` | ✅ Ativo | Controle de acesso por papel |
| `tests/__mocks__/supabase.ts` | ✅ Ativo | Mock builder com select/insert/update/delete/eq/gte/lte |

### Schema `financial_entries` (já existente — migration 011)

```sql
financial_entries (
  id            uuid PRIMARY KEY,
  business_unit text CHECK IN ('vcchic','moriel','sezo','3d_cend','3d_marketing','3d_producao','3d_inovacao','3d_negocios','3d_csc'),
  entry_type    text CHECK IN ('revenue','expense','cmv'),
  category      text,
  amount        numeric(12,2),
  description   text,
  entry_date    date,
  created_at    timestamptz,
  updated_at    timestamptz
)
```

**RLS já configurado:**
- CEO: ALL (INSERT, UPDATE, DELETE, SELECT)
- `gestor_vcchic`: SELECT apenas `business_unit IN ('vcchic','moriel','sezo')`
- Outros papéis: sem acesso (RLS bloqueia em nível de banco)

### Escopo desta story

Esta story **não** altera o cálculo de `grossProfit` em `financialService.ts` — esse ajuste fica para Story 2.8 (quando os dados históricos estiverem populados). O objetivo aqui é fornecer a **interface de registro** dos lançamentos.

---

## Acceptance Criteria

### AC-1 — `financialEntriesService.ts` com CRUD completo

- Arquivo: `services/financialEntriesService.ts`
- Exporta interfaces:
  ```typescript
  export interface FinancialEntry {
    id: string
    business_unit: string
    entry_type: 'revenue' | 'expense' | 'cmv'
    category: string | null
    amount: number
    description: string | null
    entry_date: string
    created_at: string
    updated_at: string
  }

  export interface NewFinancialEntry {
    business_unit: string
    entry_type: 'revenue' | 'expense' | 'cmv'
    category?: string
    amount: number
    description?: string
    entry_date: string
  }
  ```
- Exporta `financialEntriesService` com funções:
  - `getEntries({ businessUnit, period, entryType? })` → `Promise<ServiceResult<FinancialEntry[]>>`
    - Filtra por `business_unit`, `entry_date` no período, opcionalmente por `entry_type`
    - Ordena por `entry_date DESC`
  - `createEntry(entry: NewFinancialEntry)` → `Promise<ServiceResult<FinancialEntry>>`
    - Bloqueia silenciosamente por RLS se papel não for CEO (retorna erro do Supabase)
  - `updateEntry(id: string, updates: Partial<NewFinancialEntry>)` → `Promise<ServiceResult<FinancialEntry>>`
    - Atualiza campos permitidos; RLS bloqueia não-CEO
  - `deleteEntry(id: string)` → `Promise<ServiceResult<void>>`
    - Hard delete; RLS bloqueia não-CEO
- Toda função segue o padrão `ServiceResult<T>` de `financialService.ts`: `{ data: T | null; error: string | null }`
- Toda função tem `try/catch` — nunca propaga exceção

### AC-2 — Componente `FinancialEntriesPanel.tsx`

- Arquivo: `components/financial/FinancialEntriesPanel.tsx`
- Props:
  ```typescript
  interface FinancialEntriesPanelProps {
    businessUnit: string   // loja selecionada no dashboard ('vcchic' | 'moriel' | 'sezo' | 'consolidated')
    period: { start: string; end: string }
  }
  ```
- Renderiza:
  - Tabela de lançamentos com colunas: Data, Tipo, Categoria, Valor, Descrição, Ações (CEO only)
  - Tipos formatados: `cmv` → "CMV", `expense` → "Despesa", `revenue` → "Receita"
  - Valores formatados em BRL (`Intl.NumberFormat`)
  - Botão "Adicionar Lançamento" visível somente para `userRole === 'ceo'`
  - Estado de loading: skeleton de 3 linhas na tabela
  - Estado vazio: mensagem "Nenhum lançamento no período"
  - Estado de erro: mensagem de erro em vermelho
- Modal de criação/edição (visível somente para CEO):
  - Campos: `entry_type` (select: CMV / Despesa / Receita), `category` (text), `amount` (number, min=0.01), `description` (textarea), `entry_date` (date)
  - `business_unit` herdado da prop (não editável pelo usuário — preenchido automaticamente)
    - Quando `businessUnit === 'consolidated'`: CEO pode escolher a loja no modal (dropdown vcchic/moriel/sezo)
  - Validação obrigatória: `entry_type`, `amount > 0`, `entry_date`
  - Botão "Salvar" chama `createEntry` ou `updateEntry`
  - Botão "Cancelar" fecha o modal sem salvar
  - Feedback de sucesso/erro via toast (`sonner`)
- Ação de excluir (CEO only):
  - Botão de lixeira por linha
  - Confirmação inline: "Confirmar exclusão?" com "Sim" / "Cancelar"
  - Chama `deleteEntry`, recarrega a lista após sucesso

### AC-3 — Integração na `FinancialDashboard.tsx`

- Após os 4 KPI cards, adicionar uma seção "Lançamentos Financeiros" que renderiza `<FinancialEntriesPanel>`
- Repass as props `businessUnit` (loja selecionada no seletor) e `period` (período selecionado)
- Quando `businessUnit === 'consolidated'` (seleção CEO), painel exibe entradas de todas as 3 lojas (vcchic + moriel + sezo) — `getEntries` é chamado 3× em paralelo e resultados concatenados
- Seção separada por `<hr>` ou divisor visual com label "Lançamentos"

### AC-4 — Restrição de acesso por papel

- `gestor_vcchic`:
  - Vê a tabela de lançamentos em modo read-only (sem botão "Adicionar", sem ações de editar/excluir)
  - Vê apenas entradas de `business_unit IN ('vcchic','moriel','sezo')` — garantido pelo RLS do banco
- CEO:
  - Vê e gerencia lançamentos de todas as lojas VcChic
  - Não deve gerenciar entradas `3d_*` nesta story (escopo: Grupo VcChic apenas — Store 2.8 cobre 3D Digital)

### AC-5 — Testes unitários para `financialEntriesService`

- Arquivo: `tests/services/financialEntriesService.test.ts`
- Usa o mesmo padrão de `financialService.test.ts` (vi.mock + createMockBuilder)
- Cenários obrigatórios:
  1. `getEntries` retorna lista ordenada quando há entradas no período
  2. `getEntries` retorna array vazio quando não há entradas
  3. `createEntry` retorna a entrada criada com campos corretos
  4. `updateEntry` retorna a entrada atualizada
  5. `deleteEntry` retorna `{ data: null, error: null }` em sucesso
  6. Qualquer função retorna `{ data: null, error: '...' }` em erro de rede
- `npm test -- tests/services/financialEntriesService.test.ts` → 6/6 passando

### AC-6 — Tech debt: teste para `getGroupSummary` (herdado de 2.4)

- Em `tests/services/financialService.test.ts`, adicionar novo `describe('getGroupSummary')` com:
  1. Agrega receita e adSpend das 3 lojas corretamente (9 mocks: 3 lojas × 2 queries cada)
  2. Retorna `roas: null` quando nenhuma loja tem adSpend
  3. Retorna `{ data: null, error }` se qualquer loja falhar

---

## Tasks / Subtasks

### Task 1 — `financialEntriesService.ts`
- [x] 1.1 Criar `services/financialEntriesService.ts` com interfaces `FinancialEntry` e `NewFinancialEntry`
- [x] 1.2 Implementar `getEntries` com filtros `business_unit`, `entry_date` e `entry_type` opcional
- [x] 1.3 Implementar `createEntry` com `.insert().select().single()`
- [x] 1.4 Implementar `updateEntry` com `.update().eq('id', id).select().single()`
- [x] 1.5 Implementar `deleteEntry` com `.delete().eq('id', id)`
- [x] 1.6 Verificar: `npm run typecheck` passa

### Task 2 — `FinancialEntriesPanel.tsx`
- [x] 2.1 Criar `components/financial/FinancialEntriesPanel.tsx` com tabela e estados (loading/vazio/erro)
- [x] 2.2 Implementar modal de criação/edição com validação de campos obrigatórios
- [x] 2.3 Implementar lógica de exclusão com confirmação inline
- [x] 2.4 Implementar diferenciação CEO × gestor_vcchic (hide/show ações)
- [x] 2.5 Implementar formatação BRL para valores e label amigável para `entry_type`
- [x] 2.6 Verificar: `npm run typecheck` passa

### Task 3 — Integração em `FinancialDashboard.tsx`
- [x] 3.1 Importar `FinancialEntriesPanel` em `pages/FinancialDashboard.tsx`
- [x] 3.2 Adicionar seção "Lançamentos Financeiros" abaixo dos KPI cards
- [x] 3.3 Passar props `businessUnit` e `period` do estado do dashboard para o painel
- [x] 3.4 Verificar: `npm run typecheck` e `npm run lint` passam

### Task 4 — Testes
- [x] 4.1 Criar `tests/services/financialEntriesService.test.ts` com 6 cenários (AC-5)
- [x] 4.2 Adicionar `describe('getGroupSummary')` em `tests/services/financialService.test.ts` com 3 cenários (AC-6)
- [x] 4.3 Executar: `npm test -- tests/services/financialEntriesService.test.ts` → 6/6 passam
- [x] 4.4 Executar: `npm test -- tests/services/financialService.test.ts` → todos passam (incluindo novos)
- [x] 4.5 Executar: `npm run lint` → zero novos warnings
- [x] 4.6 Executar: `npm run typecheck` → zero erros novos

---

## Dev Notes

### Padrão de service — seguir `financialService.ts`

```typescript
// services/financialEntriesService.ts
import { supabase } from '@/lib/supabase'

type ServiceResult<T> = { data: T | null; error: string | null }

export interface FinancialEntry {
  id: string
  business_unit: string
  entry_type: 'revenue' | 'expense' | 'cmv'
  category: string | null
  amount: number
  description: string | null
  entry_date: string
  created_at: string
  updated_at: string
}

export interface NewFinancialEntry {
  business_unit: string
  entry_type: 'revenue' | 'expense' | 'cmv'
  category?: string
  amount: number
  description?: string
  entry_date: string
}

export const financialEntriesService = {
  async getEntries({
    businessUnit,
    period,
    entryType,
  }: {
    businessUnit: string
    period: { start: string; end: string }
    entryType?: 'revenue' | 'expense' | 'cmv'
  }): Promise<ServiceResult<FinancialEntry[]>> {
    try {
      let query = supabase
        .from('financial_entries')
        .select('*')
        .eq('business_unit', businessUnit)
        .gte('entry_date', period.start)
        .lte('entry_date', period.end)
        .order('entry_date', { ascending: false })

      if (entryType) query = query.eq('entry_type', entryType)

      const { data, error } = await query
      if (error) return { data: null, error: error.message }
      return { data: data ?? [], error: null }
    } catch (err: unknown) {
      return { data: null, error: err instanceof Error ? err.message : 'Erro desconhecido' }
    }
  },

  async createEntry(entry: NewFinancialEntry): Promise<ServiceResult<FinancialEntry>> {
    try {
      const { data, error } = await supabase
        .from('financial_entries')
        .insert(entry)
        .select()
        .single()
      if (error) return { data: null, error: error.message }
      return { data, error: null }
    } catch (err: unknown) {
      return { data: null, error: err instanceof Error ? err.message : 'Erro desconhecido' }
    }
  },

  async updateEntry(
    id: string,
    updates: Partial<NewFinancialEntry>
  ): Promise<ServiceResult<FinancialEntry>> {
    try {
      const { data, error } = await supabase
        .from('financial_entries')
        .update({ ...updates, updated_at: new Date().toISOString() })
        .eq('id', id)
        .select()
        .single()
      if (error) return { data: null, error: error.message }
      return { data, error: null }
    } catch (err: unknown) {
      return { data: null, error: err instanceof Error ? err.message : 'Erro desconhecido' }
    }
  },

  async deleteEntry(id: string): Promise<ServiceResult<void>> {
    try {
      const { error } = await supabase
        .from('financial_entries')
        .delete()
        .eq('id', id)
      if (error) return { data: null, error: error.message }
      return { data: null, error: null }
    } catch (err: unknown) {
      return { data: null, error: err instanceof Error ? err.message : 'Erro desconhecido' }
    }
  },
}
```

### Visibilidade de ações por papel

```tsx
// No FinancialEntriesPanel
const { userRole } = useAuth()
const canEdit = userRole === 'ceo'

// Botão "Adicionar Lançamento"
{canEdit && <button onClick={() => openModal()}>Adicionar Lançamento</button>}

// Colunas da tabela
{canEdit && <td>{/* edit/delete actions */}</td>}
```

### Formatação de valores BRL

```typescript
const formatBRL = (value: number) =>
  new Intl.NumberFormat('pt-BR', { style: 'currency', currency: 'BRL' }).format(value)

const ENTRY_TYPE_LABEL: Record<string, string> = {
  cmv: 'CMV',
  expense: 'Despesa',
  revenue: 'Receita',
}
```

### Mock para `createEntry` (insert + select + single)

O `createMockBuilder` já suporta `.insert().select().single()` — a cadeia retorna o mock builder que resolve com `response`. Verificar que o mock builder em `tests/__mocks__/supabase.ts` tem `insert`, `select`, `single` (já implementados).

### Integração no Dashboard — modo consolidado

```typescript
// Quando businessUnit === 'consolidated': buscar todas as lojas em paralelo
if (businessUnit === 'consolidated') {
  const [vcchic, moriel, sezo] = await Promise.all([
    financialEntriesService.getEntries({ businessUnit: 'vcchic', period }),
    financialEntriesService.getEntries({ businessUnit: 'moriel', period }),
    financialEntriesService.getEntries({ businessUnit: 'sezo', period }),
  ])
  // concatenar resultados
}
```

### Atenção: `eq` encadeado em `getEntries`

O Supabase não suporta múltiplos `.eq('entry_type', ...)` para OR — use `.in('entry_type', [...])` se precisar filtrar múltiplos tipos. Nesta story, `entryType` filtra um único tipo por vez.

---

## Testing

### Testes manuais pós-implementação

1. **CEO — criar lançamento:** Abrir `/financeiro` → selecionar VcChic → clicar "Adicionar Lançamento" → preencher campos → "Salvar" → entrada aparece na tabela
2. **CEO — editar lançamento:** Clicar no ícone de edição de uma linha → modal preenche com dados existentes → alterar valor → "Salvar" → tabela atualiza
3. **CEO — excluir lançamento:** Clicar na lixeira → confirmar → entrada desaparece da tabela
4. **gestor_vcchic — read-only:** Logar como gestor → painel exibe lançamentos sem botão "Adicionar" e sem ações de editar/excluir
5. **Estado vazio:** Selecionar período sem lançamentos → mensagem "Nenhum lançamento no período"

### Comandos de teste

```bash
npm test -- tests/services/financialEntriesService.test.ts
npm test -- tests/services/financialService.test.ts
npm run lint
npm run typecheck
```

---

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Debug Log References
- Nenhum desvio de implementação — story seguida conforme especificado

### Completion Notes
- 7/7 testes `financialEntriesService` passando (AC-5)
- 8/8 testes `financialService` passando, incluindo 3 novos de `getGroupSummary` (AC-6)
- 10 test files / 64 testes de projeto — todos passando (regressão limpa)
- Falhas pré-existentes em `.aiox-core/workflow-intelligence/` — não relacionadas à Story 2.5
- `getEntries` com filtro `entryType` usa cast de tipo explícito para compatibilidade TypeScript com query builder encadeado

### File List
- `services/financialEntriesService.ts` — CRIADO
- `components/financial/FinancialEntriesPanel.tsx` — CRIADO
- `pages/FinancialDashboard.tsx` — MODIFICADO (import + seção Lançamentos + hr divisor)
- `tests/services/financialEntriesService.test.ts` — CRIADO
- `tests/services/financialService.test.ts` — MODIFICADO (describe getGroupSummary com 3 cenários)
- `docs/stories/2.5.cmv-despesas-vcchic.story.md` — MODIFICADO (status + checkboxes)

### Change Log
- 2026-05-07 — Story criada por @sm (River)
- 2026-05-07 — Implementada por @dev (Dex) — claude-sonnet-4-6

---

## QA Results

**Revisor:** @qa (Quinn) — 2026-05-07
**GATE: ✅ PASS**

### Rastreabilidade
- AC-1 ✅ CRUD completo — `getEntries`, `createEntry`, `updateEntry`, `deleteEntry`; `ServiceResult<T>` em todos; `try/catch` em todos
- AC-2 ✅ `FinancialEntriesPanel` — tabela + modal + inline confirm delete + 3 estados (loading/empty/error) + BRL formatado
- AC-3 ✅ Dashboard integrado — import correto, `<hr>` divisor, props `businessUnit` e `period` repassadas; modo consolidado usa 3× parallel fetch
- AC-4 ✅ `canEdit = userRole === 'ceo'` controla visibilidade na UI; RLS garante isolamento no banco
- AC-5 ✅ 7 cenários (6 mínimos + RLS error bonus) — todos passando
- AC-6 ✅ 3 cenários `getGroupSummary` adicionados: agregação 3 lojas, roas null, erro parcial

### Segurança
- `amount > 0` validado antes de cada submissão ✅
- RLS bloqueia INSERT/UPDATE/DELETE de não-CEO no banco — testado com cenário de erro RLS ✅
- Hard delete documentado como intencional (tabela sem `deleted_at`) ✅
- `Intl.NumberFormat` para BRL — sem interpolação manual ✅
- `<input type="date">` garante formato ISO no frontend ✅

### Observações (não-bloqueantes, MEDIUM)
- `updateEntry` injeta `updated_at` no cliente — aceitável, tabela sem trigger de auto-update
- Sem testes de componente para `FinancialEntriesPanel` — consistente com estratégia do projeto (service layer only)

### Regressão
- 10 test files / 64 testes de projeto — 100% passando ✅

---

## CodeRabbit Integration

**Pre-commit scan:** `wsl bash -c 'cd /mnt/c/Users/User/projetos/nexus-os && ~/.local/bin/coderabbit --prompt-only -t uncommitted'`

**Focus areas:**
- CRUD: RLS bloqueia operações de não-CEO em nível de banco (testar cenário de erro de RLS retornado pelo Supabase)
- `deleteEntry`: hard delete sem soft delete — confirmar que é intencional (tabela não tem `deleted_at`)
- Formatação BRL: usar `Intl.NumberFormat` e não concatenar string manualmente
- Modal: validação de `amount > 0` antes de chamar `createEntry`

---

*Story 2.5 criada por @sm (River) — 2026-05-07*
