---
id: "1.4.1"
title: "Configurar ESLint + Prettier"
status: Done
epic: "EPIC-1"
sprint: "Sprint 4"
agent: "@devops"
effort: "1h"
priority: MEDIUM
created_at: "2026-03-17"
dependencies:
  - "1.3.9"
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

# Story 1.4.1 — Configurar ESLint + Prettier

**Status:** Done | **Sprint 4 — Quality & DevOps** ✅ | **Agente:** @devops | **Esforço:** 1h

---

## Story Statement

**Como** desenvolvedor do Nexus OS,
**Quero** ter ESLint e Prettier configurados e funcionando,
**Para que** o código siga padrões consistentes e problemas de qualidade sejam detectados automaticamente.

---

## Contexto

O projeto não tem linter configurado. Isso significa que erros de TypeScript, imports não usados, e inconsistências de código passam despercebidos. O ESLint também é pré-requisito para o GitHub Actions (Story 1.4.5).

[Source: docs/architecture/technical-debt-assessment.md — Qualidade de Código 4/10]

---

## Acceptance Criteria

- [x] **AC-1:** ESLint configurado com regras para TypeScript e React
- [x] **AC-2:** Prettier configurado com regras consistentes com o estilo atual do projeto
- [x] **AC-3:** `npm run lint` executa sem erro (ou com apenas warnings conhecidos)
- [x] **AC-4:** `npm run format` formata o código automaticamente
- [x] **AC-5:** `.eslintignore` e `.prettierignore` configurados para excluir `node_modules`, `.aiox/`, etc.
- [x] **AC-6:** Scripts adicionados ao `package.json`

---

## Tasks / Subtasks

- [x] **Task 1 — Instalar dependências** (AC: 1, 2)
  - `npm install -D eslint @typescript-eslint/eslint-plugin @typescript-eslint/parser eslint-plugin-react eslint-plugin-react-hooks`
  - `npm install -D prettier eslint-config-prettier eslint-plugin-prettier globals`

- [x] **Task 2 — Criar `eslint.config.js`** (AC: 1) — ESLint 9 flat config (`.eslintrc.json` não suportado no ESLint 9)
  - Browser + Node globals configurados, TypeScript + React rules

- [x] **Task 3 — Criar `.prettierrc`** (AC: 2)
  - `{ "semi": true, "singleQuote": true, "tabWidth": 2, "printWidth": 100 }` — ajustado para o estilo real do projeto

- [x] **Task 4 — Adicionar scripts ao package.json** (AC: 6)
  - `"lint"`, `"lint:fix"`, `"format"` adicionados

- [x] **Task 5 — Criar .eslintignore / .prettierignore** (AC: 5)
  - Excluem: `node_modules`, `.aiox/`, `.aiox-core/`, `dist/`, `docs/`, `squads/`, etc.

- [x] **Task 6 — Rodar e corrigir erros críticos** (AC: 3)
  - 0 erros | 74 warnings (any + unused vars — resolvidos em 1.4.2/1.4.3)
  - Corrigidos: imports `React.ReactNode` / `React.FormEvent` em 3 componentes auth

- [x] **Task 7 — Commit**
  - `git commit -m "feat: configurar ESLint + Prettier [Story 1.4.1]"`

---

## Dev Notes

### `.eslintrc.json` base

```json
{
  "extends": [
    "eslint:recommended",
    "plugin:@typescript-eslint/recommended",
    "plugin:react/recommended",
    "plugin:react-hooks/recommended",
    "prettier"
  ],
  "parser": "@typescript-eslint/parser",
  "plugins": ["@typescript-eslint"],
  "rules": {
    "react/react-in-jsx-scope": "off",
    "@typescript-eslint/no-explicit-any": "warn",
    "@typescript-eslint/no-unused-vars": ["warn", { "argsIgnorePattern": "^_" }]
  },
  "settings": {
    "react": { "version": "detect" }
  }
}
```

### Erros esperados no primeiro lint

O projeto tem:
- Múltiplos `any` em tipos (Story 1.4.2 resolve)
- `StoreMetric` duplicada (Story 1.4.3 resolve)
- Alguns imports não usados

Estratégia: corrigir erros `error` (não `warn`) nesta story. Os `warn` de `any` serão resolvidos em 1.4.2.

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## Definition of Done

- [x] ESLint e Prettier instalados e configurados
- [x] `npm run lint` executa sem erros críticos
- [x] Scripts no package.json
- [x] Commit realizado
