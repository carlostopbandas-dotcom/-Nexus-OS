---
id: "1.4.3"
title: "Consolidar StoreMetric em types.ts"
status: Done
epic: "EPIC-1"
sprint: "Sprint 4"
agent: "@dev"
effort: "30min"
priority: LOW
created_at: "2026-03-17"
dependencies:
  - "1.4.2"
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

# Story 1.4.3 — Consolidar `StoreMetric` em `types.ts`

**Status:** Done | **Sprint 4 — Quality & DevOps** ✅ | **Agente:** @dev | **Esforço:** 30min

---

## Story Statement

**Como** desenvolvedor do Nexus OS,
**Quero** ter uma única definição de `StoreMetric` em `types.ts`,
**Para que** não haja interfaces duplicadas e conflitantes entre `App.tsx` e `Dashboard.tsx`.

---

## Contexto

A interface `StoreMetric` foi definida em dois lugares: `App.tsx` e `Dashboard.tsx`. Com o tipo `number | string` para campos financeiros (sales, spend, roas), há risco de cálculos silenciosamente incorretos quando o banco retorna string e o código trata como number.

[Source: docs/architecture/db-specialist-review.md#agr-2]
[Source: docs/architecture/frontend-spec.md — UX-HIGH-2]

---

## Acceptance Criteria

- [x] **AC-1:** `StoreMetric` definida **apenas** em `types.ts`
- [x] **AC-2:** `sales`, `spend`, `roas` tipados como `number`
- [x] **AC-3:** Todas as referências a `StoreMetric` importam de `types.ts`
- [x] **AC-4:** Definições duplicadas removidas de `useAppStore.ts` e `Dashboard.tsx`
- [x] **AC-5:** `npm run typecheck` passa sem erros

---

## Tasks / Subtasks

- [x] **Task 1 — Localizar** — 2 definições: `useAppStore.ts` e `Dashboard.tsx`
- [x] **Task 2 — Consolidar em `types.ts`** — `sales/spend/roas: number`, `created_at?/updated_at?`
- [x] **Task 3 — Remover duplicatas** — `useAppStore.ts` e `Dashboard.tsx` limpos; `storeMetricsService.ts` import atualizado para `@/types`
- [x] **Task 4 — Typecheck** — sem erros
- [x] **Task 5 — Commit**

---

## Dev Notes

### Tipo seguro para campos monetários

```typescript
// O banco pode retornar como numeric (number) mas às vezes como string em edge cases
// Na camada do service, normalizar antes de retornar:
sales: typeof raw.sales === 'string' ? parseFloat(raw.sales) : (raw.sales ?? 0)
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## Definition of Done

- [x] `StoreMetric` em um único lugar (`types.ts`)
- [x] Campos financeiros como `number`
- [x] `tsc --noEmit` sem erros
- [x] Commit realizado
