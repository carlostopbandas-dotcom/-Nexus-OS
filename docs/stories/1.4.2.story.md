---
id: "1.4.2"
title: "Remover todos os `any` dos mappers TypeScript"
status: Ready
epic: "EPIC-1"
sprint: "Sprint 4"
agent: "@dev"
effort: "2h"
priority: MEDIUM
created_at: "2026-03-17"
dependencies:
  - "1.4.1"
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

# Story 1.4.2 — Remover todos os `any` dos mappers TypeScript

**Status:** Ready | **Sprint 4 — Quality & DevOps** 🟡 | **Agente:** @dev | **Esforço:** 2h

---

## Story Statement

**Como** desenvolvedor do Nexus OS,
**Quero** eliminar todos os tipos `any` explícitos do codebase TypeScript,
**Para que** o TypeScript forneça proteção real de tipos e erros sejam capturados em tempo de compilação.

---

## Contexto

O TypeScript com `any` é essencialmente JavaScript com sintaxe extra — a segurança de tipos é perdida. Os `any` foram identificados principalmente nos mappers de resposta do Supabase e em interfaces de componentes.

[Source: docs/architecture/technical-debt-assessment.md — Qualidade de Código 4/10]

---

## Acceptance Criteria

- [ ] **AC-1:** Zero ocorrências de `: any` explícito no codebase (exceto casos justificados com `// eslint-disable-next-line`)
- [ ] **AC-2:** Tipos substituídos por interfaces/types adequados de `types.ts`
- [ ] **AC-3:** `npm run lint` passa sem warnings de `@typescript-eslint/no-explicit-any`
- [ ] **AC-4:** `npm run typecheck` (`tsc --noEmit`) passa sem erros
- [ ] **AC-5:** Nenhuma regressão funcional

---

## Tasks / Subtasks

- [ ] **Task 1 — Mapear todos os `any`** (AC: 1)
  - `grep -r ": any" src/ components/ pages/ services/ store/`
  - Listar e categorizar: qual tipo correto seria

- [ ] **Task 2 — Substituir `any` em services** (AC: 1, 2)
  - Os services criados em 1.2.3 devem ter tipos corretos
  - Supabase retorna `T | null` — tipar corretamente

- [ ] **Task 3 — Substituir `any` em componentes** (AC: 1, 2)
  - Props de componentes com `any` → usar interface correta
  - Event handlers com `any` → usar `React.ChangeEvent<HTMLInputElement>`, etc.

- [ ] **Task 4 — Substituir `any` em store** (AC: 1, 2)
  - `useAppStore` deve ser completamente tipado

- [ ] **Task 5 — Rodar typecheck** (AC: 4)
  - `npx tsc --noEmit`
  - Corrigir erros de tipo encontrados

- [ ] **Task 6 — Commit**
  - `git commit -m "fix: remover any explícito e tipificar corretamente o codebase [Story 1.4.2]"`

---

## Dev Notes

### Padrões comuns de substituição

```typescript
// Supabase query response
// ❌ const { data }: { data: any } = await supabase...
// ✅ const { data }: { data: Lead[] | null } = await supabase...

// Event handler
// ❌ onChange={(e: any) => setValue(e.target.value)}
// ✅ onChange={(e: React.ChangeEvent<HTMLInputElement>) => setValue(e.target.value)}

// Props genéricos
// ❌ function Component({ data }: { data: any })
// ✅ function Component<T>({ data }: { data: T })
```

### Casos onde `any` pode ser mantido com justificativa

```typescript
// eslint-disable-next-line @typescript-eslint/no-explicit-any
const config = JSON.parse(rawConfig) as any  // JSON externo sem schema conhecido
```

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## Definition of Done

- [ ] Zero `any` explícito sem justificativa
- [ ] `npm run lint` sem warnings de `any`
- [ ] `tsc --noEmit` sem erros
- [ ] Commit realizado
