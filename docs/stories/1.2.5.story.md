---
id: "1.2.5"
title: "Criar migration 003_schema_hardening.sql"
status: Ready
epic: "EPIC-1"
sprint: "Sprint 2"
agent: "@data-engineer"
effort: "3h"
priority: HIGH
created_at: "2026-03-17"
dependencies:
  - "1.1.3"
clickup:
  task_id: ""
  epic_task_id: ""
  list: "Backlog"
  url: ""
  last_sync: ""
---

# Story 1.2.5 — Criar migration `003_schema_hardening.sql`

**Status:** Ready | **Sprint 2 — Architecture & DB Foundation** 🟠 | **Agente:** @data-engineer | **Esforço:** 3h

---

## Story Statement

**Como** Carlos (CEO / único usuário do Nexus OS),
**Quero** que o schema do banco seja reforçado com `updated_at` em todas as tabelas, constraints de validação, e FK para `call_logs.lead_id`,
**Para que** os dados tenham integridade referencial, timestamps de auditoria, e validações no nível do banco (não apenas no código).

---

## Contexto

O schema atual (capturado em `001_initial_schema.sql`) tem múltiplos problemas estruturais:
- Sem `updated_at` em 7 tabelas (impossível saber quando um registro foi modificado)
- Sem FK entre `call_logs.lead_name` e `leads.id` (integridade referencial zero)
- Sem CHECK constraints (dados inválidos podem ser inseridos diretamente via API)

Esta migration adiciona estas melhorias sem alterar dados existentes.

[Source: docs/architecture/db-specialist-review.md#33-migration-003--schema-hardening]
[Source: docs/architecture/technical-debt-assessment.md#td-c7-td-c8]

---

## Acceptance Criteria

- [ ] **AC-1:** `updated_at` adicionado nas 7 tabelas com `DEFAULT now()`
- [ ] **AC-2:** Trigger `set_updated_at()` criado e aplicado em todas as 7 tabelas
- [ ] **AC-3:** `call_logs.lead_id uuid` adicionado como coluna FK opcional (`ON DELETE SET NULL`)
- [ ] **AC-4:** `leads.email` recebe constraint UNIQUE
- [ ] **AC-5:** CHECK constraints adicionados: `leads.status`, `tasks.type`, `okrs.progress`, `okrs.key_results`
- [ ] **AC-6:** Arquivo `supabase/migrations/003_schema_hardening.sql` criado e commitado
- [ ] **AC-7:** Dados existentes não são afetados (migration é aditiva, não destrutiva)
- [ ] **AC-8:** Script de rollback `supabase/rollbacks/rollback_003.sql` criado

---

## Tasks / Subtasks

- [ ] **Task 1 — Criar `003_schema_hardening.sql`** (AC: 1, 2, 3, 4, 5, 6)
  - Usar SQL exato do `db-specialist-review.md` Seção 3.3
  - Revisar cada ALTER TABLE antes de executar

- [ ] **Task 2 — Criar script de rollback** (AC: 8)
  - Criar `supabase/rollbacks/rollback_003.sql`
  - Usar o rollback do `db-specialist-review.md` Seção 4

- [ ] **Task 3 — Testar em DEV** (AC: 7)
  - Executar migration em ambiente de desenvolvimento
  - Verificar que dados existentes continuam acessíveis
  - Verificar que `updated_at` está populado nas rows existentes (deve ser `now()` no momento da migration)

- [ ] **Task 4 — Aplicar em produção** (AC: 1-5)
  - Via Supabase Dashboard → SQL Editor
  - Verificar que trigger funciona: update um lead e confirmar que `updated_at` muda

- [ ] **Task 5 — Validar constraints** (AC: 4, 5)
  - Testar que inserção de `leads.status` inválido é rejeitada
  - Testar que inserção de `tasks.type` inválido é rejeitada
  - Testar que `okrs.progress` fora de 0-100 é rejeitado

- [ ] **Task 6 — Commit** (AC: 6)
  - `git add supabase/migrations/003_schema_hardening.sql supabase/rollbacks/rollback_003.sql`
  - `git commit -m "feat: migration 003 schema hardening (updated_at, FK, constraints) [Story 1.2.5]"`

---

## Dev Notes

### SQL completo da migration

```sql
-- supabase/migrations/003_schema_hardening.sql
-- DEPENDÊNCIA: 002_security_foundation.sql deve ter sido aplicado

-- 1. Adicionar updated_at em todas as tabelas
ALTER TABLE leads         ADD COLUMN IF NOT EXISTS updated_at timestamptz DEFAULT now();
ALTER TABLE tasks         ADD COLUMN IF NOT EXISTS updated_at timestamptz DEFAULT now();
ALTER TABLE events        ADD COLUMN IF NOT EXISTS updated_at timestamptz DEFAULT now();
ALTER TABLE call_logs     ADD COLUMN IF NOT EXISTS updated_at timestamptz DEFAULT now();
ALTER TABLE okrs          ADD COLUMN IF NOT EXISTS updated_at timestamptz DEFAULT now();
ALTER TABLE content_posts ADD COLUMN IF NOT EXISTS updated_at timestamptz DEFAULT now();
ALTER TABLE store_metrics ADD COLUMN IF NOT EXISTS updated_at timestamptz DEFAULT now();

-- 2. Trigger function para auto-updated_at
CREATE OR REPLACE FUNCTION set_updated_at()
RETURNS TRIGGER LANGUAGE plpgsql AS $$
BEGIN
  NEW.updated_at = now();
  RETURN NEW;
END;
$$;

-- 3. Aplicar trigger em todas as tabelas
DO $$
DECLARE t text;
BEGIN
  FOREACH t IN ARRAY ARRAY['leads','tasks','events','call_logs','okrs','content_posts','store_metrics']
  LOOP
    EXECUTE format(
      'CREATE TRIGGER %I_updated_at BEFORE UPDATE ON %I FOR EACH ROW EXECUTE FUNCTION set_updated_at()',
      t, t
    );
  END LOOP;
END $$;

-- 4. FK lead_id em call_logs (nova coluna — lead_name permanece por compatibilidade)
ALTER TABLE call_logs
  ADD COLUMN IF NOT EXISTS lead_id uuid REFERENCES leads(id) ON DELETE SET NULL;

-- 5. UNIQUE em leads.email
ALTER TABLE leads ADD CONSTRAINT leads_email_unique UNIQUE (email);

-- 6. CHECK constraints
ALTER TABLE leads ADD CONSTRAINT leads_status_valid
  CHECK (status IS NULL OR status IN ('New','Contacted','Qualified','Proposal','Won','Lost'));

ALTER TABLE tasks ADD CONSTRAINT tasks_type_valid
  CHECK (type IN ('Big Rock','Medium','Small'));

ALTER TABLE okrs ADD CONSTRAINT okrs_progress_range
  CHECK (progress >= 0 AND progress <= 100);

ALTER TABLE okrs ADD CONSTRAINT okrs_key_results_array
  CHECK (key_results IS NULL OR jsonb_typeof(key_results) = 'array');
```

[Source: docs/architecture/db-specialist-review.md#33-migration-003--schema-hardening]

### Nota sobre leads.email UNIQUE

Pode haver emails duplicados nos dados existentes. Antes de aplicar a constraint UNIQUE:
```sql
-- Verificar duplicatas antes de aplicar:
SELECT email, COUNT(*) FROM leads GROUP BY email HAVING COUNT(*) > 1;
```
Se houver duplicatas, limpar manualmente antes da migration.

### lead_name permanece

A coluna `call_logs.lead_name` (TEXT livre) permanece após esta migration para compatibilidade com o código existente. O backfill do `lead_id` é feito na Story 1.2.6.

[Source: docs/architecture/db-specialist-review.md#34-migration-004]

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled

---

## Definition of Done

- [ ] `003_schema_hardening.sql` criado e aplicado
- [ ] `updated_at` presente nas 7 tabelas
- [ ] Trigger `set_updated_at` funcionando (testar update de um lead)
- [ ] `call_logs.lead_id` coluna criada
- [ ] CHECK constraints ativas
- [ ] Script de rollback criado
- [ ] Commit realizado
