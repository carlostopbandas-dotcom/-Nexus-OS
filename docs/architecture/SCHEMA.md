# Database Schema — Nexus OS
> Generated by @data-engineer (Brownfield Discovery Phase 2) — 2026-03-16
> Schema inferido via análise de código (App.tsx, types.ts, pages/*.tsx)

## Visão Geral

**Database:** Supabase (PostgreSQL)
**Connection:** Anon key client-side (sem auth layer)
**Schema versioning:** ❌ Não existe (sem supabase/migrations/)

---

## Tabelas

### `leads`
CRM: prospects e clientes dos produtos 3D Digital.

| Coluna      | Tipo       | Nullable | Observação                          |
|-------------|-----------|----------|-------------------------------------|
| id          | uuid       | NOT NULL | PK (presumido)                      |
| name        | text       | NOT NULL | Nome do lead                        |
| email       | text       | nullable | ❌ Deveria ser NOT NULL + unique     |
| source      | text       | nullable | 'Organic' \| 'Paid' \| 'Indication' |
| status      | text       | nullable | Enum: LeadStatus                    |
| value       | numeric    | nullable | Valor potencial do negócio (R$)     |
| product     | text       | nullable | Nexus, Mapa da Clareza, etc.        |
| created_at  | timestamptz| NOT NULL | ✅ Presente                         |
| updated_at  | —          | —        | ❌ AUSENTE                          |

**Issues:** email nullable, sem updated_at, sem UK em email.

---

### `tasks`
Tarefas diárias seguindo a regra 1-3-5 (Big Rock / Medium / Small).

| Coluna      | Tipo       | Nullable | Observação                    |
|-------------|-----------|----------|-------------------------------|
| id          | uuid       | NOT NULL | PK                            |
| title       | text       | NOT NULL | Descrição da tarefa           |
| type        | text       | NOT NULL | 'Big Rock' \| 'Medium' \| 'Small' |
| completed   | boolean    | NOT NULL | Status de conclusão           |
| category    | text       | nullable | BusinessUnit                  |
| created_at  | timestamptz| NOT NULL | ✅ Presente                   |
| updated_at  | —          | —        | ❌ AUSENTE                    |

---

### `events`
Calendário e agenda do CEO.

| Coluna      | Tipo       | Nullable | Observação                       |
|-------------|-----------|----------|----------------------------------|
| id          | uuid       | NOT NULL | PK                               |
| title       | text       | NOT NULL | Título do evento                 |
| start_time  | text       | NOT NULL | HH:mm ou ISO string              |
| end_time    | text       | nullable | HH:mm ou ISO string              |
| type        | text       | nullable | 'meeting' \| 'deep_work' \| etc. |
| attendees   | text[]     | nullable | Array de nomes (sem FK)          |
| day_offset  | integer    | nullable | 0=hoje, 1=amanhã, etc.           |
| created_at  | timestamptz| nullable | ✅ Presumido                     |
| updated_at  | —          | —        | ❌ AUSENTE                       |

**Issues:** start_time/end_time como text (deveria ser timestamptz). attendees sem FK para leads.

---

### `call_logs`
Registro de calls de vendas, mentoria e diagnóstico.

| Coluna              | Tipo       | Nullable | Observação                          |
|---------------------|-----------|----------|-------------------------------------|
| id                  | uuid       | NOT NULL | PK                                  |
| lead_name           | text       | NOT NULL | ❌ DESNORMALIZADO — deveria ser FK  |
| date                | text       | nullable | String livre (ex: "Hoje, 15:00")    |
| duration            | text       | nullable | String livre (ex: "35 min")         |
| type                | text       | nullable | 'Discovery' \| 'Closing' \| etc.    |
| status              | text       | nullable | 'Completed' \| 'Missed' \| 'Scheduled' |
| sentiment           | text       | nullable | 'Positive' \| 'Neutral' \| 'Negative' |
| transcript_snippet  | text       | nullable | ⚠️ PII — transcrição de conversa   |
| summary             | text       | nullable | Resumo gerado pelo CEO/AI           |
| recording_url       | text       | nullable | URL do recording (mock)             |
| created_at          | timestamptz| nullable | ✅ Presumido                        |
| updated_at          | —          | —        | ❌ AUSENTE                          |

**Issues CRÍTICOS:**
- `lead_name` é TEXT livre, sem FK para `leads.id`
- `date` e `duration` são strings livres (sem tipagem forte)
- `transcript_snippet` contém PII sem proteção RLS

---

### `okrs`
Objectives & Key Results por unidade de negócio.

| Coluna      | Tipo       | Nullable | Observação                        |
|-------------|-----------|----------|-----------------------------------|
| id          | uuid       | NOT NULL | PK                                |
| unit        | text       | NOT NULL | BusinessUnit                      |
| objective   | text       | NOT NULL | Descrição do objetivo             |
| progress    | integer    | NOT NULL | 0-100 (percentual)                |
| key_results | jsonb      | nullable | ❌ Array de {text, completed} sem schema |
| created_at  | timestamptz| nullable | ✅ Presumido                      |
| updated_at  | —          | —        | ❌ AUSENTE                        |

**Issues:** key_results em JSONB sem constraints — qualquer estrutura pode ser inserida.

---

### `content_posts`
Posts de conteúdo para LinkedIn, Instagram e Newsletter.

| Coluna       | Tipo       | Nullable | Observação                    |
|--------------|-----------|----------|-------------------------------|
| id           | uuid       | NOT NULL | PK                            |
| platform     | text       | NOT NULL | 'linkedin' \| 'instagram' \| 'newsletter' |
| content      | text       | NOT NULL | Corpo do post                 |
| image_prompt | text       | nullable | Prompt para geração de imagem |
| status       | text       | nullable | 'Draft' \| 'Scheduled' \| 'Published' |
| date         | text       | nullable | Data de publicação            |
| stats        | jsonb      | nullable | {likes, views}                |
| created_at   | timestamptz| nullable | ✅ Presumido                  |
| updated_at   | —          | —        | ❌ AUSENTE                    |

---

### `store_metrics`
Métricas de e-commerce por loja (Grupo VcChic).

| Coluna     | Tipo         | Nullable | Observação                           |
|------------|-------------|----------|--------------------------------------|
| id         | uuid         | NOT NULL | PK                                   |
| store_name | text         | NOT NULL | Nome da loja                         |
| sales      | numeric\|text| nullable | ⚠️ Tipo inconsistente (number\|string) |
| spend      | numeric\|text| nullable | ⚠️ Tipo inconsistente (number\|string) |
| roas       | numeric\|text| nullable | ⚠️ Tipo inconsistente (number\|string) |
| date       | date         | NOT NULL | Data da métrica                      |
| created_at | timestamptz  | nullable | ❌ Não confirmado                    |
| updated_at | —            | —        | ❌ AUSENTE                           |

---

## Relacionamentos Identificados

```
leads (id) ←── ❌ SEM FK ──── call_logs (lead_name: TEXT)
leads (id) ←── ❌ SEM FK ──── events (attendees: TEXT[])
```

**Nenhuma FK definida no schema atual.** Integridade referencial depende 100% do código da aplicação.

---

## Padrões de Acesso (Access Patterns)

| Query | Frequência | Otimização Atual |
|-------|-----------|-----------------|
| `leads` ORDER BY created_at DESC | Alta | ❌ Sem índice confirmado |
| `tasks` ORDER BY created_at DESC | Alta | ❌ Sem índice confirmado |
| `events` ORDER BY start_time ASC | Alta | ❌ Sem índice confirmado |
| `call_logs` ORDER BY created_at DESC | Alta | ❌ Sem índice confirmado |
| `store_metrics` WHERE date >= firstDay | Média | ❌ Sem índice em date |
| `leads` WHERE id = $1 (UPDATE) | Alta | ✅ PK (automático) |
